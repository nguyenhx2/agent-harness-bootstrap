# /review-changes

Run the two reviewers over the current diff, then rebuild the code graph with
python .claude/scripts/code-graph.py and refresh the HTML view with
python .claude/scripts/graph-html.py before merging.
